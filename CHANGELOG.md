# 変更履歴

このファイルは利用者向けのリリースノートです。fork を「Sync fork」で更新する前にご確認ください。

---

## v9.2.0 - 2026-07-09

**DB 変更あり**（migration 0021）。Sync fork → Cloudflare Workers Builds の自動デプロイで本番 D1 にも自動適用されます（追加の設定は不要）。

### 新機能

- **開催回ごとの「中止」に対応しました**（定期イベントの特定回だけを休みにできます）。
  - 中止した回は募集・リマインド・ノルマ確認などの配信対象から外れます。中止は記録として残り（墓石方式）、あとから確認できます。
  - 通知タブで、予定されている配信を**開催回ごとに事前確認**できるようになりました（まだ送っていない配信の予定表示）。
- **臨時回（イレギュラーな追加開催）を登録できるようになりました。**
  - 定期スケジュール外の単発開催を追加でき、その回にも通常どおり募集・リマインドが流れます。
  - 臨時回には **補足メッセージ（note）** を添えられ、募集メッセージに差し込まれます（「今回は特別会場です」等の一言を添えられます）。
- **募集の手動操作を強化しました。**
  - 送信済みの募集を、必要なときに**手動で再送（force）**できるようになりました。
  - リマインド・ノルマ確認の **DM の見出しにサーバー名を付記**しました。複数サーバーで同じ Bot を使っていても、どのサーバーの通知かがひと目で分かります。

### 改善

- **通知タブの表示を全面的に見直しました。**
  - ステータスの言葉を「予定 / 開催中 / 終了 / 中止」に整理し、配色（ピル）を分かりやすくしました。
  - 未来の予定について、**表示件数を選べるセレクト**を追加しました（既定は「次の1回」）。
  - ステータスや通知種類の**絞り込み条件をブラウザに記憶**するようにし、開き直しても前回の表示が保たれます。
  - チェックボックスの並びが崩れる不具合を修正しました。
  - 配信の一時停止／再開ボタンの文言を「配信する／配信しない」から **「再開する／中止する」** に変更し、動作が直感的に分かるようにしました。

### バグ修正

- 手動募集が一度失敗したあと、同じ日にもう一度実行しようとすると `409`（重複）エラーで再試行できなかった問題を修正しました。

### 内部改善（動作への影響なし）

- Cloudflare Workers 無料プランの CPU 制限（1 呼び出し 10ms）に収まるよう、毎分の定期処理を軽量化しました（定期スケジュールの展開を毎回→1日1回に集約、処理の分割実行、クエリ削減）。多数の定期イベントを登録していても定期処理が安定して完走します。

---

## v9.1.0 - 2026-07-06

**DB 変更なし**。Sync fork → 自動デプロイのみで適用されます（追加の設定は不要）。

### 新機能・改善

- **「📊 状況確認」の表示を見やすくしました**
  - 先頭に **「👤 あなたの回答」** を表示。人数が多くても、自分がどう回答したかが一目で分かります。
  - 名前の一覧は小さな文字（subtext）で表示し、視覚的なノイズを抑えました。
  - 単発イベントの「📊 全候補の状況」でも、候補ごとに自分の回答が表示されます。
- **管理UI（`/manage`）を React ベースに全面刷新**しました。画面構成・機能は従来と同等のまま、デザインを統一し操作性を改善しています。
- セットアップガイド（setup.html）の図版・手順説明を改善しました。

---

## v9.0.0 - 2026-07-04

**DB 変更あり**（migration 0014〜0020）。**破壊的変更あり**。Sync fork → Cloudflare Workers Builds の自動デプロイで本番 D1 にも適用されます。**更新前に必ず下記「⚠️ 更新前に必ずお読みください」を確認してください。**

### ⚠️ 更新前に必ずお読みください

- **【最重要】既存の利用者は Build variable `DB_NAME` の設定が必要です。**
  このバージョンから、同一 Cloudflare アカウントに複数のインスタンスを接続しても DB が混ざらないよう、**D1 名を Worker 名から自動導出**するようになりました。**既存の利用者が何も設定せずに更新すると、Bot が新しい空の DB に接続し「データが消えた」ように見えます**（以前の DB は残っているため設定を追加すれば元に戻ります）。
  更新する前に、fork を接続した Cloudflare Worker の **Settings → Build → Build variables and secrets** に、これまで使っていた D1 名を明示してください:

  ```
  DB_NAME = choiemu-event-bot-db
  ```

  （新規に導入する方は設定不要です。fork 接続時に Worker 名から DB が自動作成されます。）

- **番号割り当て機能を撤去しました。** `assignments` テーブルが削除されます（migration 0015・非可逆）。番号割り当てをお使いだった方は、代わりに「メンバー配置（グループ分け）」機能をご利用ください。行頭ラベル・連番・カスタム Prefix で同等以上のことができます。

### 新機能

- **メンバー配置（グループ分け）の大幅強化**
  - 手動配置・グループ内の順序組み換え・NG（別グループ）リスト・チャンネル投稿の改善。
  - **グループごとの連番開始番号**を指定可能に（migration 0018）。区切り記号「：」の表示を全体で統一。
  - **行頭ラベル**を開催回 × メンバー単位で盤面から直接編集できるように（migration 0017）。ラベルはメンバーに追従し、連番を消費しません。
  - **カスタム Prefix**（例: `Leader：`）を配置の投稿に付与可能に（migration 0016）。
  - **配置結果の投稿先チャンネルをマスター設定化**＋専用の「メンバー配置設定」ページを追加（migration 0019）。
- **通知編集に sticky リアルタイムプレビュー**を追加。編集中にチャンネル投稿・グループ分けの見え方をその場で確認できます。
- **通知メニューを 2 つに分離**: 「通知設定」（マスター）と「通知」（開催回を横断するトランザクション一覧・種類フィルター付き）。
- **区分（segment）のロール指定をプルダウン化**＋入力検証＋「対象 0 名で同期しようとした場合」の警告。

### 改善

- **表示名をサーバー（ギルド）ごとに保持**するように変更（migration 0020）。同一メンバーが複数サーバーに参加していても、名前を互いに上書きし合わなくなりました。
- メンバー配置の保存モデルを「配置＝明示保存／構造＝即時確定」に統一（ドラッグ後の保存挙動が予測しやすくなりました）。
- メンバーカードに順序番号（1..N）を表示、「自動配置」ボタンのラベルを実挙動に合わせて明示化、そのほか配置画面の UX 改善。
- 配布ガイド（`setup.html`）に「更新手順」タブを追加。

### バグ修正

- **締切告知が日付をまたぐたびに毎日再送されていた不具合を修正。** 開催日までに日付が変わるたび締切告知が再送されていた問題を、開催回につき 1 回だけ送るよう修正しました。
- グループ分けのサーバー応答に一部識別子（uuid）が欠落していた箇所の残修正。

### 内部変更（利用者影響なし・参考情報）

- D1 マイグレーション `0014`〜`0020` を追加。
- D1 名の自動導出スクリプト（`scripts/derive-db-name.mjs`）を `npm run deploy` の冒頭に追加（優先順: `DB_NAME` ＞ Worker 名からの導出 ＞ 既定値）。

---

## v8.2.1 - 2026-06-28

**DB 変更なし**。

### 改善

- ペア制約一覧の「必須／推奨」バッジの視覚優先度を逆転: 必須を赤の塗りで強く、推奨をデフォルトの控えめ pill に変更（旧実装では推奨が緑で目立ち、優先度が逆に見えていた）。

---

## v8.2.0 - 2026-06-28

**DB 変更なし**。

### 新機能

- 番号割り当ての子ページに「📣 結果をチャンネルへ投稿」ボタンを追加。グループ分けと同じく、現在保存されている番号割り当てを通知チャンネルへ整形して投稿できます。
  - サーバー側エンドポイント: `POST /occurrences/<uuid>/assignments/announce`

---

## v8.1.0 - 2026-06-28

**DB 変更なし**。

### 改善

- 番号割り当てをグループ分けと同じく独立した子ページに分離（URL: `…/occurrences/<uuid>/assignment`）。開催回一覧の下に表が割り込んで見える挙動を撤廃し、戻る/進む・直リンク・複数開催回の切り替えがしやすくなりました。

---

## v8.0.3 - 2026-06-28

**DB 変更なし**。

### 改善

- 番号割り当ての「早い順で割り当て」「ランダムで割り当て」を両方プライマリーボタンに統一（どちらも主アクションのため）。

---

## v8.0.2 - 2026-06-28

**DB 変更なし**。グループ分け画面の UI 微調整。

### 改善

- 未割り当てカラムのカードから「新規参加」バッジを除去（未割り当てに居ること自体が同義のため）。
- ペア制約違反のハイライトを「必須＞推奨」の重み付けに整理: 必須は強い赤の塗り＋太枠、推奨は破線の細枠に変更（以前は色違いだけで推奨の方が目立って見えるケースがあったため）。

---

## v8.0.1 - 2026-06-28

**DB 変更なし**。グループ分け画面で counter のズレ・制約違反の誤判定・名前変更失敗・保存後の配置消失を一括解消する重要修正です。Sync fork でそのまま受け取れます。

### バグ修正

- **グループ分け画面の全症状の真因を修正**
  - サーバーが返すグループ情報に `uuid` が欠落していたため、UI 側で全グループが同じ識別子として扱われていました。これが counter ズレ・apart 制約が常に違反表示・グループ名変更で `400` エラー・保存後の配置消失といった連鎖症状の根本原因でした。1 か所修正することで、これまでの v8.0.0 で残っていた症状もすべて解消されます。
- レスポンス形状を固定化する回帰テストを追加し、同種の欠落を CI で検出できるようにしました。

---

## v8.0.0 - 2026-06-28

**DB 変更あり**（migration 0010〜0013 を追加）。Sync fork → Cloudflare Workers Builds の自動デプロイで本番 D1 にも適用されます。新規環境変数の追加はありません。

### 新機能

- **グループ分け機能を追加**
  - 開催回ごとに参加者を任意のグループに割り振り、結果をチャンネルへ投稿できます。
  - **自動配置**（バランス考慮の自動振り分け）と **手動ドラッグ&ドロップ**の両対応。
  - **ペア制約**: 「同じグループに」「別のグループに」を、必須/推奨の強さで指定できます（通知単位で共有）。
  - 制約違反は画面上でハイライト＋アラート表示されます。
- **番号割り当てに 2 モードを追加**
  - **早い順**: 出欠回答が早かった順に番号を割り当て
  - **ランダム**: 公平なシャッフルで番号を割り当て
- **番号割り当て / グループ分けを標準機能化**
  - 以前は通知設定でオプトインだった機能を、すべての通知で常時利用可能に。
- **通知オーバーホール**
  - 回答締切・送信時刻・送信ログ・ペース配信を一括で扱える UI に刷新。
  - 送信履歴はサーバー（guild）単位に分離。
- **Discord スラッシュコマンドの整理**
  - `/recruit` → `/notify` に改名（選択 UI 付き）。
  - `/help` を「使い方ガイド」として刷新。
- **配布ガイド (setup.html) の改修**
  - 暗色テーマ（Midnight）へ刷新。
  - 「ADMIN_TOKEN を忘れたとき」セクションを新設。
  - 文言を自然な表現に整理。

### 改善

- 管理 UI を Discord 風のデザインシステムへ全面刷新（サーフェス階層、フラット化、2 トーン、サイドメニュー sticky、ServerRail にサーバーアイコン表示など）。
- フォーム系モーダルを「本物の子ページ遷移」化。ハッシュルーティング＋ UUID 連動で、URL を直接共有・戻る/進む操作にも対応。
- 履歴テーブルの絞り込みフィルタを各カラム単位に整理し、全体検索は撤去。
- 送信履歴の宛先列を表示名で表示（以前は user_id がそのまま出ていた箇所を解消）。
- 送信量バナーは超過時のみ表示。

### バグ修正

- **グループ分け画面の重大バグを一括修正**（v8.0.0 直前に発見・修正）
  - 未保存状態で「保存」を押すと、グループに割り当てたメンバーが全員「未割り当て」に戻ってしまう不具合を修正。
  - グループ名を変更しようとすると `400 group_uuid and name required` エラーが出る場合がある不具合を修正。
  - 制約を追加・削除すると、未保存のドラッグ&ドロップ配置が消えてしまう不具合を修正。
  - 配置を変えても制約違反のアラートが古いままになる場合がある不具合を修正。
  - ペア制約一覧で、現在の参加者でないユーザーが user_id 文字列のまま表示されていた箇所を、登録済みの表示名にフォールバックするよう修正。
- 履歴テーブルの絞り込み欄で日本語（IME）が打てない不具合を修正。
- ハッシュルータが画面遷移しない場合がある不具合を修正。
- 送信履歴タブが hidden のまま表示されない不具合を修正。
- 配布ガイドの細かな表示崩れ（sticky ヘッダ周辺の余白、サブステップ番号の見た目）を修正。

### 内部変更（利用者影響なし・参考情報）

- DB に UUID シャドウキーを追加（6 テーブル）し、Admin API ルートを UUID 受信に切り替え。
- D1 マイグレーション `0010`〜`0013` を追加（通知オーバーホール、グループ分けスキーマ、UUID シャドウキー、番号割り当て/グループ分け常時化）。
- Discord Interactions 検証を `discord-interactions` から Web Crypto API の Ed25519 へ置換。
- `wrangler.jsonc` を全ブランチ共通化（`env.staging` 上書き方式）。`database_id` は配布 base から省略し、Workers Builds 側で自動生成。

---

## v7.1.0 およびそれ以前

過去のリリースは git log を参照してください。
